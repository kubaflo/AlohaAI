<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AlohaAI.ViewModels"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             x:Class="AlohaAI.Views.ProfilePage"
             x:DataType="vm:ProfileViewModel"
             Title="Profile"
             Shell.NavBarIsVisible="False"
             SafeAreaEdges="None">
    <Grid>
        <!-- Layer 0: Background Image -->
        <Image Source="bg_sunset.png" Aspect="AspectFill" InputTransparent="True" />
        <!-- Layer 1: Dark overlay for readability -->
        <BoxView Color="#2A000000" InputTransparent="True" />

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                           HorizontalOptions="Center" VerticalOptions="Center"
                           Color="{StaticResource Primary}" ZIndex="99" />
    <ScrollView>
        <VerticalStackLayout x:Name="MainContent" Spacing="0">

            <!-- Gradient Header -->
            <Border StrokeShape="RoundRectangle 0,0,24,24"
                    Stroke="Transparent"
                    Padding="20,70,20,24">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#805B9FE6" Offset="0.0" />
                        <GradientStop Color="#807B68AE" Offset="0.5" />
                        <GradientStop Color="#802D1B69" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
                <VerticalStackLayout Spacing="16">
                    <!-- Avatar & Level -->
                    <HorizontalStackLayout Spacing="16" HorizontalOptions="Center">
                        <Border StrokeShape="RoundRectangle 28"
                                Stroke="#FFFFFF40"
                                StrokeThickness="2"
                                WidthRequest="56" HeightRequest="56"
                                BackgroundColor="#FFFFFF20">
                            <Image Source="mascot.png" WidthRequest="40" HeightRequest="40"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                            <Label Text="{Binding Level, StringFormat='Level {0}'}"
                                   FontSize="22" FontAttributes="Bold" TextColor="White" />
                            <Label Text="{Binding TotalXp, StringFormat='{0} XP earned'}"
                                   FontSize="14" TextColor="#FFD54F" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>

                    <!-- Horizontal Stats Row -->
                    <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="8">
                        <VerticalStackLayout HorizontalOptions="Center" Spacing="2">
                            <Label Text="{Binding CurrentStreak}" FontSize="22" FontAttributes="Bold"
                                   TextColor="White" HorizontalOptions="Center" />
                            <HorizontalStackLayout HorizontalOptions="Center" Spacing="3">
                                <Image Source="icon_clock.png" WidthRequest="14" HeightRequest="14" VerticalOptions="Center" />
                                <Label Text="Streak" FontSize="11" TextColor="#FFFFFFB0" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center" Spacing="2">
                            <Label Text="{Binding BestStreak}" FontSize="22" FontAttributes="Bold"
                                   TextColor="White" HorizontalOptions="Center" />
                            <HorizontalStackLayout HorizontalOptions="Center" Spacing="3">
                                <Image Source="icon_trophy.png" WidthRequest="14" HeightRequest="14" VerticalOptions="Center" />
                                <Label Text="Best" FontSize="11" TextColor="#FFFFFFB0" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="2" HorizontalOptions="Center" Spacing="2">
                            <Label Text="{Binding LessonsCompleted}" FontSize="22" FontAttributes="Bold"
                                   TextColor="White" HorizontalOptions="Center" />
                            <HorizontalStackLayout HorizontalOptions="Center" Spacing="3">
                                <Image Source="icon_notebook.png" WidthRequest="14" HeightRequest="14" VerticalOptions="Center" />
                                <Label Text="Lessons" FontSize="11" TextColor="#FFFFFFB0" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="3" HorizontalOptions="Center" Spacing="2">
                            <Label Text="{Binding TotalXp}" FontSize="22" FontAttributes="Bold"
                                   TextColor="White" HorizontalOptions="Center" />
                            <HorizontalStackLayout HorizontalOptions="Center" Spacing="3">
                                <Image Source="icon_xp.png" WidthRequest="14" HeightRequest="14" VerticalOptions="Center" />
                                <Label Text="XP" FontSize="11" TextColor="#FFFFFFB0" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Padding="20,20,20,0" Spacing="16">

                <!-- Achievements -->
                <Label Text="Achievements" FontSize="18" FontAttributes="Bold" TextColor="White" />

                <CollectionView ItemsSource="{Binding Achievements}" SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" Span="3" HorizontalItemSpacing="8" VerticalItemSpacing="8" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="vm:AchievementItem">
                            <Border Style="{StaticResource GlassCard}" Padding="10" HeightRequest="110">
                                <VerticalStackLayout Spacing="6" VerticalOptions="Center" HorizontalOptions="Center">
                                    <Image Source="{Binding Icon}" WidthRequest="32" HeightRequest="32"
                                           Opacity="{Binding IconOpacity}" HorizontalOptions="Center" />
                                    <Label Text="{Binding Title}" FontSize="11" FontAttributes="Bold"
                                           TextColor="{Binding TextColor}" HorizontalOptions="Center"
                                           HorizontalTextAlignment="Center" MaxLines="2" LineBreakMode="WordWrap" />
                                    <Label Text="{Binding StatusText}" FontSize="10"
                                           TextColor="{Binding DescColor}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Path Progress Breakdown -->
                <Label Text="Path Progress" FontSize="18" FontAttributes="Bold" TextColor="White" />

                <CollectionView ItemsSource="{Binding PathProgress}" SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="vm:PathProgressItem">
                            <Border Style="{StaticResource GlassCard}" Padding="16">
                                <VerticalStackLayout Spacing="8">
                                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                                        <Image Source="{Binding IconImage}" WidthRequest="28" HeightRequest="28" VerticalOptions="Center" />
                                        <Label Grid.Column="1" Text="{Binding Title}" FontSize="15"
                                               FontAttributes="Bold" TextColor="White" VerticalOptions="Center" />
                                        <Label Grid.Column="2" Text="{Binding ProgressText}" FontSize="13"
                                               TextColor="{StaticResource Gray300}" VerticalOptions="Center" />
                                    </Grid>
                                    <Grid HeightRequest="6">
                                        <Border BackgroundColor="#20FFFFFF"
                                                StrokeShape="RoundRectangle 3" Stroke="Transparent" />
                                        <Border BackgroundColor="{Binding BarColor}"
                                                StrokeShape="RoundRectangle 3" Stroke="Transparent"
                                                HorizontalOptions="Start"
                                                WidthRequest="{Binding Progress, Converter={StaticResource ProgressToWidthConverter}, ConverterParameter=300}">
                                            <Border.Shadow>
                                                <Shadow Brush="{Binding BarColor}" Offset="0,0" Radius="6" Opacity="0.6" />
                                            </Border.Shadow>
                                        </Border>
                                    </Grid>
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Settings Link -->
                <Border Style="{StaticResource GlassCard}" Padding="16">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding OpenSettingsCommand}" />
                    </Border.GestureRecognizers>
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                        <Image Source="icon_explore.png" WidthRequest="20" HeightRequest="20" VerticalOptions="Center" />
                        <Label Grid.Column="1" Text="Settings" FontSize="16" TextColor="White" VerticalOptions="Center" />
                        <Label Grid.Column="2" Text="→" FontSize="16" TextColor="{StaticResource Gray400}" VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- About -->
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center" Margin="0,8,0,80">
                    <Label Text="AlohaAI v1.0.0" FontSize="12" TextColor="{StaticResource Gray500}" HorizontalOptions="Center" />
                    <Label Text="Open source • MIT License" FontSize="12" TextColor="{StaticResource Gray500}" HorizontalOptions="Center" />
                </VerticalStackLayout>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
    </Grid>
</ContentPage>
