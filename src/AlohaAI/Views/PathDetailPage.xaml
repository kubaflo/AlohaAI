<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AlohaAI.ViewModels"
             x:Class="AlohaAI.Views.PathDetailPage"
             x:DataType="vm:PathDetailViewModel"
             Title="{Binding Title}"
             BackgroundColor="{StaticResource Gray900}">
    <Grid>
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                           HorizontalOptions="Center" VerticalOptions="Center"
                           Color="{StaticResource Primary}" />
    <ScrollView>
        <VerticalStackLayout x:Name="MainContent" Padding="20" Spacing="16">

            <!-- Path Header -->
            <VerticalStackLayout Spacing="4" Margin="0,0,0,0">
                <Label Text="{Binding Title}" FontSize="26" FontAttributes="Bold" />
                <Label Text="{Binding PathDescription}" FontSize="14" Opacity="0.6" />
            </VerticalStackLayout>

            <!-- Path Progress Summary -->
            <Border BackgroundColor="{StaticResource CardDark}"
                    StrokeShape="RoundRectangle 12" Stroke="Transparent" Padding="16">
                <VerticalStackLayout Spacing="8">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Overall Progress" FontSize="14" FontAttributes="Bold" />
                        <Label Grid.Column="1" Text="{Binding OverallProgressText}"
                               FontSize="14" Opacity="0.6" />
                    </Grid>
                    <Grid HeightRequest="8">
                        <Border BackgroundColor="{StaticResource Gray700}"
                                StrokeShape="RoundRectangle 4" Stroke="Transparent" />
                        <Border BackgroundColor="{Binding PathColor}"
                                StrokeShape="RoundRectangle 4" Stroke="Transparent"
                                HorizontalOptions="Start"
                                WidthRequest="{Binding OverallProgress, Converter={StaticResource ProgressToWidthConverter}, ConverterParameter=320}" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Modules -->
            <CollectionView ItemsSource="{Binding Modules}" SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="16" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:ModuleDisplayItem">
                        <Border BackgroundColor="{StaticResource CardDark}"
                                StrokeShape="RoundRectangle 16"
                                Stroke="Transparent"
                                Padding="16">
                            <VerticalStackLayout Spacing="12">
                                <!-- Module Header -->
                                <Grid ColumnDefinitions="*,Auto">
                                    <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold" />
                                    <Label Grid.Column="1" Text="{Binding ProgressText}"
                                           FontSize="14" Opacity="0.5" VerticalOptions="Center" />
                                </Grid>
                                <Label Text="{Binding Description}" FontSize="12" Opacity="0.6" />

                                <!-- Lessons List -->
                                <CollectionView ItemsSource="{Binding Lessons}" SelectionMode="None">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate x:DataType="vm:LessonDisplayItem">
                                            <Grid ColumnDefinitions="Auto,*,Auto" Padding="0,6" ColumnSpacing="12">
                                                <Grid.GestureRecognizers>
                                                    <TapGestureRecognizer
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PathDetailViewModel}}, Path=SelectLessonCommand}"
                                                        CommandParameter="{Binding .}" />
                                                </Grid.GestureRecognizers>
                                                <Label Text="{Binding StatusIcon}" FontSize="16" VerticalOptions="Center" />
                                                <Label Grid.Column="1" Text="{Binding Title}" FontSize="15" VerticalOptions="Center" />
                                                <Label Grid.Column="2" Text="{Binding Xp, StringFormat='+{0} XP'}"
                                                       FontSize="12" TextColor="{StaticResource XPColor}" VerticalOptions="Center" />
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>

                                <!-- Quiz Button -->
                                <Button Text="📝 Take Quiz"
                                        IsVisible="{Binding HasQuiz}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PathDetailViewModel}}, Path=StartQuizCommand}"
                                        CommandParameter="{Binding Id}"
                                        BackgroundColor="{StaticResource Secondary}"
                                        TextColor="White"
                                        CornerRadius="10"
                                        FontAttributes="Bold"
                                        HeightRequest="44" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
    </Grid>
</ContentPage>
