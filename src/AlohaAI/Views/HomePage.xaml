<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AlohaAI.ViewModels"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             x:Class="AlohaAI.Views.HomePage"
             x:DataType="vm:HomeViewModel"
             Title="Home"
             Shell.NavBarIsVisible="False"
             BackgroundColor="Transparent"
             SafeAreaEdges="None">
    <Grid BackgroundColor="Transparent" SafeAreaEdges="None">
        <!-- Layer 0: Background Image -->
        <Image Source="bg_sunset.png" Aspect="AspectFill" InputTransparent="True" />

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                           HorizontalOptions="Center" VerticalOptions="Center"
                           Color="{StaticResource Primary}" ZIndex="99" />
    <ScrollView>
        <VerticalStackLayout x:Name="MainContent" Spacing="0" SafeAreaEdges="None">

            <!-- Header: Settings gear (top right) -->
            <Grid Padding="24,60,24,0" ColumnDefinitions="*,Auto" SafeAreaEdges="None">
                <BoxView Color="Transparent" />
                <Border Grid.Column="1" BackgroundColor="#D01A1035" StrokeShape="RoundRectangle 14"
                        Stroke="Transparent" WidthRequest="40" HeightRequest="40">
                    <Border.Shadow>
                        <Shadow Brush="#000000" Offset="0,2" Radius="8" Opacity="0.3" />
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateToSettingsCommand}" />
                    </Border.GestureRecognizers>
                    <Image Source="icon_settings.png" WidthRequest="22" HeightRequest="22"
                           HorizontalOptions="Center" VerticalOptions="Center" Opacity="0.9" />
                </Border>
            </Grid>

            <!-- AlohaAI Logo -->
            <Image Source="logo_alohaai.png" HeightRequest="40"
                   Aspect="AspectFit" HorizontalOptions="Center"
                   Margin="0,8,0,8" />

            <!-- Welcome text -->
            <Label Text="Aloha! Welcome back!"
                   FontSize="24"
                   FontFamily="OpenSansSemibold"
                   TextColor="#DDFFFFFF"
                   HorizontalTextAlignment="Center"
                   Padding="24,0,24,36">
                <Label.Shadow>
                    <Shadow Brush="#40000000" Offset="0,1" Radius="4" Opacity="0.4" />
                </Label.Shadow>
            </Label>

            <!-- Learning Paths as rich cards (BindableLayout) -->
            <VerticalStackLayout Padding="16,0,16,100" Spacing="14"
                                 BindableLayout.ItemsSource="{Binding PathItems}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="vm:HomePathItem">
                        <Border StrokeShape="RoundRectangle 18" StrokeThickness="0.8" Padding="16,14">
                            <Border.BackgroundColor>
                                <Color>#E81A1035</Color>
                            </Border.BackgroundColor>
                            <Border.Stroke>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#40FFFFFF" Offset="0.0" />
                                    <GradientStop Color="#08FFFFFF" Offset="1.0" />
                                </LinearGradientBrush>
                            </Border.Stroke>
                            <Border.Shadow>
                                <Shadow Brush="#000000" Offset="0,8" Radius="24" Opacity="0.5" />
                            </Border.Shadow>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:HomeViewModel}}, Path=NavigateToPathCommand}"
                                    CommandParameter="{Binding Id}" />
                            </Border.GestureRecognizers>
                            <VerticalStackLayout Spacing="8">
                                <!-- Row 1: Icon + Title + Trophy Score -->
                                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                                    <Border BackgroundColor="{Binding Color}" StrokeShape="RoundRectangle 24"
                                            Stroke="Transparent" WidthRequest="48" HeightRequest="48">
                                        <Border.Shadow>
                                            <Shadow Brush="{Binding Color}" Offset="0,2" Radius="14" Opacity="0.65" />
                                        </Border.Shadow>
                                        <Image Source="{Binding IconImage}" WidthRequest="28" HeightRequest="28"
                                               HorizontalOptions="Center" VerticalOptions="Center" />
                                    </Border>
                                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                                        <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"
                                               FontFamily="OpenSansSemibold" TextColor="White" />
                                        <Label Text="{Binding ProgressText}" FontSize="13"
                                               TextColor="#BBBBBB" />
                                    </VerticalStackLayout>
                                    <HorizontalStackLayout Grid.Column="2" VerticalOptions="Start" Spacing="3" Margin="0,4,0,0">
                                        <Image Source="icon_trophy.png" WidthRequest="18" HeightRequest="18"
                                               VerticalOptions="Center" />
                                        <Label FontSize="14" FontAttributes="Bold" TextColor="#FFD54F"
                                               VerticalOptions="Center">
                                            <Label.Text>
                                                <MultiBinding StringFormat="{}{0}/{1}">
                                                    <Binding Path="CompletedLessons" />
                                                    <Binding Path="TotalLessons" />
                                                </MultiBinding>
                                            </Label.Text>
                                        </Label>
                                    </HorizontalStackLayout>
                                </Grid>

                                <!-- Row 2: Tags -->
                                <HorizontalStackLayout Spacing="6">
                                    <Border BackgroundColor="#30FFD700" StrokeShape="RoundRectangle 10"
                                            Stroke="Transparent" Padding="10,4">
                                        <Label Text="Course" FontSize="10" TextColor="#FFD700"
                                               FontAttributes="Bold" />
                                    </Border>
                                    <Border BackgroundColor="#305B8FD4" StrokeShape="RoundRectangle 10"
                                            Stroke="Transparent" Padding="10,4">
                                        <Label Text="{Binding LevelTag}" FontSize="10" TextColor="#8BB8F4"
                                               FontAttributes="Bold" />
                                    </Border>
                                </HorizontalStackLayout>

                                <!-- Row 3: Progress bar -->
                                <Grid HeightRequest="6">
                                    <Border BackgroundColor="#18FFFFFF" StrokeShape="RoundRectangle 3"
                                            Stroke="Transparent" />
                                    <Border StrokeShape="RoundRectangle 3" Stroke="Transparent"
                                            HorizontalOptions="Start"
                                            BackgroundColor="{Binding Color}"
                                            WidthRequest="{Binding Progress, Converter={StaticResource ProgressToWidthConverter}, ConverterParameter=300}">
                                        <Border.Shadow>
                                            <Shadow Brush="{Binding Color}" Offset="0,0" Radius="4" Opacity="0.5" />
                                        </Border.Shadow>
                                    </Border>
                                </Grid>

                                <!-- Row 4: Continue hint -->
                                <Label Text="{Binding ContinueText}" FontSize="12" TextColor="#999999"
                                       MaxLines="1" LineBreakMode="TailTruncation"
                                       IsVisible="{Binding HasContinueText}" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
    </Grid>
</ContentPage>
